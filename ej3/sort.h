#ifndef _SORT_H
#define _SORT_H


void sortphrase(char l[], char s[], unsigned int indexes[], unsigned int length);
/*
    Sort the array 'l' using the indexes of the array 'indexes' and saves it in the array 's'
*/

#endif